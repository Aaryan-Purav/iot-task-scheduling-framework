# iot-task-scheduling-framework
We built a Java simulation that benchmarks 11 CPU scheduling algorithms on a mixed-criticality IoT workload — modelling real sensor types like health alerts, motion detectors, camera processors, and background loggers, all competing for CPU time simultaneously.
